# Nginx rate-limiting config for /v2 admin panel
# Add to http {} context (top of sites-enabled/siteguard):
limit_req_zone $binary_remote_addr zone=admin_rl:10m rate=5r/m;
limit_req_status 429;

# Add inside /v2 location block:
# limit_req zone=admin_rl burst=5 nodelay;
